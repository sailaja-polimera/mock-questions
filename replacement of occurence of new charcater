#include <stdio.h>
int main()
{
  char str[20];
  char a,b;
  scanf("%s",str);
  scanf(" %c %c",&a,&b);
  for(int i=0;str[i]!='\0';i++)
  {
      if(str[i]==a)
      str[i]=b;
  }
  printf("after replacing the chracter the string is:%s",str);
	return 0;
}
